[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

[functions."*"]
  included_files = ["netlify/lib/**"]

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
